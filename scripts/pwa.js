/**
 * Obsoleto: no enlazar en HTML. Toda la lógica PWA está en scripts/pwa-install.js
 * (service worker, beforeinstallprompt y modal post-pedido). Este archivo se mantiene
 * solo para no romper enlaces históricos en documentación.
 */
