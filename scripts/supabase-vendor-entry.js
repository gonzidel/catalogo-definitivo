/**
 * Punto de entrada para empaquetar @supabase/supabase-js como bundle IIFE.
 * Bundle generado: window.fylSupabase = { createClient } (mismo origen → Safari iOS).
 * Cargar como <script defer src="scripts/vendor/supabase-js.bundle.min.js?v=...">.
 */
export { createClient } from "@supabase/supabase-js";
